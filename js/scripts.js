async function main() {
    // This is where the code you're actually experimenting with goes.
    
    // print("Hello, world!");
    // let name = await input("Please enter your name: ");

    // Boolean
    let myBoolean = true; // True or false, yes or no, on or off.

    // Number 
    let myNumber = 3.14; // Numbers in JavaScript can be either integers or decimal values.

    // String
    let myString = "Hello"; // Strings are collections of characters delimited by quotation marks.
    
    /* Arithmetic Operators
    Concatenation Operator: + (Combine two string values)
    Addition Operator: + (Add two number values)
    [Which operation takes place depends on the data type of the operands]
    Subtraction Operator: - (Subtract the second number from the first number)
    Multiplication Operator: * (Multiply two number values)
    Division Operator: / (Divide the first number by the second number)
    Modulus Operator: % (Get the remainder of a given integer division)
    */

    /* Casing 
    this-is-an-example-of-kebab-case

    thisIsAnExampleOfCamelCase (usually the default for JS variables)

    ThisIsAnExampleOfPascalCase (usually the default for JS classes)

    this_is_an_example_of_snake_case

    THIS_IS_AN_EXAMPLE_OF_SCREAMING_SNAKE_CASE
    */

    /* Relational Operators
    > Greater Than?
    < Less Than?
    >= Greater Than, Or Equal To?
    <= Less Than, or Equal To?
    != Not Equal To
    == Equal To
    === Identity
    */

    /* Logical Operators
    && And (Returns true if both operands are true, false otherwise)
    || Or (Returns true if either operand is true, false if both are false)
    ! Not (Reverses a boolean value)
    ^ Xor (Returns true if exactly one operand is true)
    */

    print(5 === "5");
}
main();
